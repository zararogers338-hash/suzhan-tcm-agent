name: Deep release rehearsal
run-name: "deep release rehearsal ${{ inputs.version || github.run_number }}"

on:
  workflow_dispatch:
    inputs:
      version:
        description: "Override test version, e.g. 1.3.5-test.1"
        required: false
        type: string
      run_packaged_e2e:
        description: "Install the exact candidate version and run packaged browser e2e"
        required: false
        type: boolean
        default: false
      run_os_smoke:
        description: "Install the exact candidate on Linux, macOS, Windows, and musl"
        required: false
        type: boolean
        default: false
      run_scientific_canary:
        description: "Run the packaged scientific capability matrix"
        required: false
        type: boolean
        default: false

concurrency: npm-test-rehearsal

permissions:
  contents: read

jobs:
  version:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    permissions:
      contents: read
    outputs:
      version: ${{ steps.version.outputs.version }}
      source: ${{ steps.version.outputs.source }}
    steps:
      - name: Require the protected default branch
        shell: bash
        run: |
          set -euo pipefail
          if [[ "${GITHUB_REPOSITORY,,}" != "synthetic-sciences/openscience" || "$GITHUB_REF" != "refs/heads/main" ]]; then
            echo "::error::npm test publishing uses registry credentials and must be dispatched from synthetic-sciences/OpenScience main; received $GITHUB_REPOSITORY at $GITHUB_REF."
            exit 1
          fi
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
      - id: version
        shell: bash
        env:
          INPUT_VERSION: ${{ inputs.version }}
        run: |
          set -euo pipefail
          source="$(git rev-parse HEAD)"
          if [ -n "$INPUT_VERSION" ]; then
            version="$INPUT_VERSION"
          else
            # Match the production release script: workspace package versions
            # intentionally stay static between releases, so the public stable
            # tag is the only authoritative base for the next candidate.
            current="$(npm view @synsci/openscience@latest version)"
            if ! [[ "$current" =~ ^[0-9]+\.[0-9]+\.[0-9]+$ ]]; then
              echo "::error::Could not resolve the current stable OpenScience version; received '$current'"
              exit 1
            fi
            core="${current%%-*}"
            IFS=. read -r major minor patch <<< "$core"
            version="${major}.${minor}.$((patch + 1))-test.${GITHUB_RUN_ID}"
          fi

          if ! [[ "$version" =~ ^[0-9]+\.[0-9]+\.[0-9]+-test\.[0-9A-Za-z.-]+$ ]]; then
            echo "::error::Test publish versions must look like 1.3.5-test.1, received '$version'"
            exit 1
          fi

          echo "version=$version" >> "$GITHUB_OUTPUT"
          echo "source=$source" >> "$GITHUB_OUTPUT"
          echo "Preparing immutable npm test version $version from $source"

  # Builds the CLI and packs the 15-package candidate on one runner. Both
  # immutable caches keep their keys, so a resume of a version reuses them.
  build-cli:
    needs: version
    runs-on: ubuntu-latest
    timeout-minutes: 90
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          fetch-depth: 0
          ref: ${{ needs.version.outputs.source }}
      - uses: ./.github/actions/setup-bun
      - uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7.0.0
        with:
          node-version: "24"
      - name: Restore immutable npm test artifacts
        id: npm-artifacts-cache
        uses: actions/cache/restore@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: .release/npm-test/${{ needs.version.outputs.version }}
          key: npm-test-artifacts-v2-${{ needs.version.outputs.source }}-${{ needs.version.outputs.version }}
      - name: Restore immutable CLI build
        id: cli-build-cache
        if: steps.npm-artifacts-cache.outputs.cache-hit != 'true'
        uses: actions/cache/restore@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: |
            backend/cli/dist
            frontend/workspace/dist/version.json
          key: npm-test-cli-v2-${{ needs.version.outputs.source }}-${{ needs.version.outputs.version }}
      - name: Build all npm CLI packages
        if: steps.npm-artifacts-cache.outputs.cache-hit != 'true' && steps.cli-build-cache.outputs.cache-hit != 'true'
        run: ./backend/cli/script/build.ts
        env:
          OPENSCIENCE_CHANNEL: test
          OPENSCIENCE_VERSION: ${{ needs.version.outputs.version }}
          OPENSCIENCE_ARTIFACT_SOURCE: ${{ needs.version.outputs.source }}
          GH_TOKEN: ${{ github.token }}
      - name: Cache immutable CLI build
        if: steps.npm-artifacts-cache.outputs.cache-hit != 'true' && steps.cli-build-cache.outputs.cache-hit != 'true'
        uses: actions/cache/save@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: |
            backend/cli/dist
            frontend/workspace/dist/version.json
          key: npm-test-cli-v2-${{ needs.version.outputs.source }}-${{ needs.version.outputs.version }}
      - name: Fail closed if an uncached version is occupied
        if: steps.npm-artifacts-cache.outputs.cache-hit != 'true'
        run: ./tooling/repo/npm-test-release.ts assert-empty
        env:
          OPENSCIENCE_VERSION: ${{ needs.version.outputs.version }}
          OPENSCIENCE_RELEASE_SOURCE: ${{ needs.version.outputs.source }}
          OPENSCIENCE_ARTIFACT_SOURCE: ${{ needs.version.outputs.source }}
      - name: Pack the complete 15-package candidate once
        if: steps.npm-artifacts-cache.outputs.cache-hit != 'true'
        run: ./tooling/repo/prepare-npm.ts
        env:
          HUSKY: "0"
          OPENSCIENCE_CHANNEL: test
          OPENSCIENCE_VERSION: ${{ needs.version.outputs.version }}
          OPENSCIENCE_RELEASE_SOURCE: ${{ needs.version.outputs.source }}
          OPENSCIENCE_ARTIFACT_SOURCE: ${{ needs.version.outputs.source }}
          OPENSCIENCE_NPM_ARTIFACT_DIR: .release/npm-test/${{ needs.version.outputs.version }}
      - name: Verify immutable manifest and packed module exports
        run: ./tooling/repo/verify-npm-artifacts.ts
        env:
          OPENSCIENCE_VERSION: ${{ needs.version.outputs.version }}
          OPENSCIENCE_ARTIFACT_SOURCE: ${{ needs.version.outputs.source }}
          OPENSCIENCE_NPM_ARTIFACT_DIR: .release/npm-test/${{ needs.version.outputs.version }}
      - name: Cache immutable source, version, integrity, and tarballs
        if: steps.npm-artifacts-cache.outputs.cache-hit != 'true'
        uses: actions/cache/save@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: .release/npm-test/${{ needs.version.outputs.version }}
          key: npm-test-artifacts-v2-${{ needs.version.outputs.source }}-${{ needs.version.outputs.version }}
      # Every gate job installs from these tarballs through a localhost
      # registry (.github/actions/candidate-registry) instead of from
      # registry.npmjs.org, so the rehearsal never waits for the registry to
      # commit fifteen uploads. The real staging happens once, in publish.yml.
      - name: Share the packed candidate with the gate jobs
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: npm-candidate-${{ needs.version.outputs.version }}
          path: .release/npm-test/${{ needs.version.outputs.version }}
          if-no-files-found: error
          compression-level: 0
          retention-days: 1

  # Proves the npm credentials and the version's availability without
  # uploading anything. publish.yml stages the release itself.
  npm-preflight:
    needs:
      - version
      - build-cli
    runs-on: ubuntu-latest
    timeout-minutes: 15
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          fetch-depth: 0
          ref: ${{ needs.version.outputs.source }}
      - uses: ./.github/actions/setup-bun
      - uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7.0.0
        with:
          node-version: "24"
          registry-url: "https://registry.npmjs.org"
      - name: Verify npm publisher
        run: ./tooling/repo/npm-release.ts preflight
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}
      - name: Fail closed if the candidate version is occupied
        run: ./tooling/repo/npm-test-release.ts assert-empty
        env:
          OPENSCIENCE_VERSION: ${{ needs.version.outputs.version }}
          OPENSCIENCE_RELEASE_SOURCE: ${{ needs.version.outputs.source }}
          OPENSCIENCE_ARTIFACT_SOURCE: ${{ needs.version.outputs.source }}

  packaged-e2e:
    needs:
      - version
      - build-cli
    if: inputs.run_packaged_e2e
    runs-on: ubuntu-latest
    timeout-minutes: 60
    defaults:
      run:
        shell: bash
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          ref: ${{ needs.version.outputs.source }}
      - uses: ./.github/actions/setup-bun
      - uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7.0.0
        with:
          node-version: "24"
      - name: Set Playwright browser path
        run: echo "PLAYWRIGHT_BROWSERS_PATH=$RUNNER_TEMP/ms-playwright" >> "$GITHUB_ENV"
      - name: Resolve Playwright version
        id: playwright
        working-directory: frontend/workspace
        run: echo "version=$(bun -e 'console.log(require("@playwright/test/package.json").version)')" >> "$GITHUB_OUTPUT"
      - name: Cache Playwright browsers
        id: playwright-cache
        uses: actions/cache@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: ${{ runner.temp }}/ms-playwright
          key: ${{ runner.os }}-${{ runner.arch }}-playwright-${{ steps.playwright.outputs.version }}-chromium
      - name: Install Playwright browsers
        if: steps.playwright-cache.outputs.cache-hit != 'true'
        working-directory: frontend/workspace
        run: bunx playwright install --with-deps chromium
      - name: Install Playwright system dependencies
        if: steps.playwright-cache.outputs.cache-hit == 'true'
        working-directory: frontend/workspace
        run: bunx playwright install-deps chromium
      - name: Install and verify Linux sandbox
        run: |
          sudo apt-get update
          sudo apt-get install --yes bubblewrap
          # Ubuntu 24.04's host-wide AppArmor policy blocks unprivileged user
          # namespaces on the hosted runner before bubblewrap can apply our
          # stricter per-process profile. This runner is disposable; enable
          # user namespaces for the job, then prove the sandbox can start.
          if [[ -e /proc/sys/kernel/apparmor_restrict_unprivileged_userns ]]; then
            echo 0 | sudo tee /proc/sys/kernel/apparmor_restrict_unprivileged_userns
          fi
          bwrap --ro-bind / / --dev /dev --proc /proc --unshare-pid --die-with-parent -- true
      - uses: ./.github/actions/candidate-registry
        with:
          version: ${{ needs.version.outputs.version }}
      - name: Install the packed OpenScience candidate
        run: |
          set -euo pipefail
          prefix="$RUNNER_TEMP/openscience-npm-test-prefix"
          home="$RUNNER_TEMP/openscience-npm-test-home"
          mkdir -p "$prefix" "$home"
          npm install --global --prefix "$prefix" "@synsci/openscience@${{ needs.version.outputs.version }}"
          echo "$prefix/bin" >> "$GITHUB_PATH"
          "$prefix/bin/openscience" --version | tee "$RUNNER_TEMP/openscience-version.txt"
          grep -Fx "${{ needs.version.outputs.version }}" "$RUNNER_TEMP/openscience-version.txt"
          wrapper_dir="$prefix/lib/node_modules/@synsci/openscience"
          if WRAPPER_DIR="$wrapper_dir" node -e 'require.resolve("@synsci/atlas/package.json", { paths: [process.env.WRAPPER_DIR] })' 2>/dev/null; then
            echo "::error::@synsci/openscience still installs the discontinued @synsci/atlas companion"
            exit 1
          fi

          launcher_prefix="$RUNNER_TEMP/synsci-npm-test-prefix"
          npm install --global --prefix "$launcher_prefix" "synsci@${{ needs.version.outputs.version }}"
          node --check "$launcher_prefix/lib/node_modules/synsci/bin/synsci.mjs"
          LAUNCHER_DIR="$launcher_prefix/lib/node_modules/synsci" node --input-type=module - <<'NODE'
          const mod = await import(`file://${process.env.LAUNCHER_DIR}/lib/channel.mjs`)
          const spec = mod.opensciencePackageSpec(process.env.OPENSCIENCE_VERSION)
          if (spec !== "@synsci/openscience@test") throw new Error(`synsci@test resolves ${spec}`)
          console.log(`synsci@test resolves ${spec}`)
          NODE

          sdk_probe="$RUNNER_TEMP/openscience-sdk-probe"
          mkdir -p "$sdk_probe"
          npm install --prefix "$sdk_probe" \
            "@synsci/sdk@${{ needs.version.outputs.version }}" \
            "@synsci/plugin@${{ needs.version.outputs.version }}"
          cat > "$sdk_probe/probe.mjs" <<'NODE'
          await import("@synsci/sdk")
          await import("@synsci/sdk/client")
          await import("@synsci/sdk/server")
          await import("@synsci/sdk/v2")
          await import("@synsci/sdk/v2/client")
          await import("@synsci/sdk/v2/server")
          await import("@synsci/plugin")
          await import("@synsci/plugin/tool")
          console.log("published SDK and plugin exports resolve")
          NODE
          node "$sdk_probe/probe.mjs"
        env:
          HOME: ${{ runner.temp }}/openscience-npm-test-home
          OPENSCIENCE_VERSION: ${{ needs.version.outputs.version }}
      - name: Set isolated OpenScience paths
        run: |
          PASS="ci-$(openssl rand -hex 16)"
          {
            echo "OPENSCIENCE_SERVER_PASSWORD=$PASS"
            echo "VITE_OPENSCIENCE_SERVER_PASSWORD=$PASS"
            echo "OPENSCIENCE_E2E_ROOT=$RUNNER_TEMP/openscience-e2e"
            echo "OPENSCIENCE_TEST_HOME=$RUNNER_TEMP/openscience-e2e/home"
            echo "OPENSCIENCE_CONFIG_DIR=$RUNNER_TEMP/openscience-e2e/config/openscience"
            echo "OPENSCIENCE_DATA_DIR=$RUNNER_TEMP/openscience-e2e/data"
            echo "OPENSCIENCE_E2E_PROJECT_DIR=$GITHUB_WORKSPACE"
            echo "OPENSCIENCE_E2E_RUNTIME=$(command -v bun)"
            echo "XDG_DATA_HOME=$RUNNER_TEMP/openscience-e2e/share"
            echo "XDG_CACHE_HOME=$RUNNER_TEMP/openscience-e2e/cache"
            echo "XDG_CONFIG_HOME=$RUNNER_TEMP/openscience-e2e/config"
            echo "XDG_STATE_HOME=$RUNNER_TEMP/openscience-e2e/state"
          } >> "$GITHUB_ENV"
          cat > frontend/workspace/.env.local <<ENV
          VITE_OPENSCIENCE_SERVER_HOST=127.0.0.1
          VITE_OPENSCIENCE_SERVER_PORT=4096
          VITE_OPENSCIENCE_SERVER_USERNAME=openscience
          VITE_OPENSCIENCE_SERVER_PASSWORD=$PASS
          ENV
      - name: Start deterministic E2E model
        run: |
          CONFIG=$(bun frontend/workspace/script/e2e-fake-model.ts --port 4097 --print-config)
          {
            printf '%s\n' "OPENSCIENCE_CONFIG_CONTENT=$CONFIG"
            printf '%s\n' "OPENSCIENCE_E2E_MODEL=e2e/echo"
            printf '%s\n' "OPENSCIENCE_E2E_FAKE_MODEL=1"
          } >> "$GITHUB_ENV"
          bun frontend/workspace/script/e2e-fake-model.ts --port 4097 > "$RUNNER_TEMP/openscience-fake-model.log" 2>&1 &
          for _ in $(seq 1 30); do
            curl -fsS "http://127.0.0.1:4097/health" > /dev/null && exit 0
            sleep 1
          done
          echo "::error::deterministic E2E model never became healthy on 127.0.0.1:4097"
          exit 1
      - name: Seed OpenScience data
        working-directory: backend/cli
        run: bun script/seed-e2e.ts
        env:
          OPENSCIENCE_DISABLE_SHARE: "true"
          OPENSCIENCE_DISABLE_LSP_DOWNLOAD: "true"
          OPENSCIENCE_DISABLE_DEFAULT_PLUGINS: "true"
          OPENSCIENCE_EXPERIMENTAL_DISABLE_FILEWATCHER: "true"
          OPENSCIENCE_TEST_HOME: ${{ env.OPENSCIENCE_TEST_HOME }}
          XDG_DATA_HOME: ${{ env.XDG_DATA_HOME }}
          XDG_CACHE_HOME: ${{ env.XDG_CACHE_HOME }}
          XDG_CONFIG_HOME: ${{ env.XDG_CONFIG_HOME }}
          XDG_STATE_HOME: ${{ env.XDG_STATE_HOME }}
          OPENSCIENCE_E2E_PROJECT_DIR: ${{ github.workspace }}
          OPENSCIENCE_E2E_SESSION_TITLE: "E2E Session"
          OPENSCIENCE_E2E_MESSAGE: "Seeded for UI e2e"
          OPENSCIENCE_E2E_MODEL: "e2e/echo"
      - name: Run published OpenScience server
        run: openscience --print-logs --log-level WARN serve --port 4096 > "$RUNNER_TEMP/openscience-server.log" 2>&1 &
        env:
          OPENSCIENCE_DISABLE_SHARE: "true"
          OPENSCIENCE_DISABLE_LSP_DOWNLOAD: "true"
          OPENSCIENCE_DISABLE_DEFAULT_PLUGINS: "true"
          OPENSCIENCE_EXPERIMENTAL_DISABLE_FILEWATCHER: "true"
          OPENSCIENCE_TEST_HOME: ${{ env.OPENSCIENCE_TEST_HOME }}
          XDG_DATA_HOME: ${{ env.XDG_DATA_HOME }}
          XDG_CACHE_HOME: ${{ env.XDG_CACHE_HOME }}
          XDG_CONFIG_HOME: ${{ env.XDG_CONFIG_HOME }}
          XDG_STATE_HOME: ${{ env.XDG_STATE_HOME }}
          OPENSCIENCE_CLIENT: "app"
      - name: Wait for published OpenScience server
        run: |
          for _ in $(seq 1 45); do
            if curl -fsS "http://127.0.0.1:4096/global/health" > /dev/null; then
              exit 0
            fi
            sleep 1
          done
          echo "::error::published OpenScience server never became healthy on 127.0.0.1:4096"
          exit 1
      - name: Run packaged E2E
        working-directory: frontend/workspace
        run: bun run test:e2e:packaged
        env:
          CI: true
          PLAYWRIGHT_BASE_URL: "http://127.0.0.1:4096"
          PLAYWRIGHT_SERVER_HOST: "127.0.0.1"
          PLAYWRIGHT_SERVER_PORT: "4096"
          VITE_OPENSCIENCE_SERVER_HOST: "127.0.0.1"
          VITE_OPENSCIENCE_SERVER_PORT: "4096"
          OPENSCIENCE_CLIENT: "app"
      - name: Print server logs
        if: failure()
        run: |
          cat "$RUNNER_TEMP/openscience-server.log" || true
          cat "$RUNNER_TEMP/openscience-fake-model.log" || true
      - name: Upload packaged E2E artifacts
        if: always()
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: npm-test-packaged-e2e-${{ github.run_attempt }}
          if-no-files-found: ignore
          retention-days: 7
          path: |
            frontend/workspace/e2e/test-results
            frontend/workspace/e2e/playwright-report
            ${{ runner.temp }}/openscience-server.log
            ${{ runner.temp }}/openscience-fake-model.log

  os-smoke:
    needs:
      - version
      - build-cli
    if: inputs.run_os_smoke
    strategy:
      fail-fast: false
      matrix:
        include:
          - name: linux-x64
            runner: ubuntu-latest
          - name: linux-arm64
            runner: ubuntu-24.04-arm
          - name: macos
            runner: macos-latest
          - name: windows
            runner: windows-latest
    # publish.yml's rehearsal gate matches these names exactly.
    name: os-smoke (${{ matrix.name }})
    runs-on: ${{ matrix.runner }}
    timeout-minutes: 25
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          ref: ${{ needs.version.outputs.source }}
      - uses: ./.github/actions/setup-bun
        with:
          install: "false"
      - uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7.0.0
        with:
          node-version: "24"
      - uses: ./.github/actions/candidate-registry
        with:
          version: ${{ needs.version.outputs.version }}
      - name: Install and smoke the OpenScience candidate
        if: runner.os != 'Windows'
        shell: bash
        run: |
          set -euo pipefail
          prefix="$RUNNER_TEMP/openscience-prefix"
          mkdir -p "$prefix"
          export OPENSCIENCE_DISABLE_SHARE=true
          export OPENSCIENCE_DISABLE_LSP_DOWNLOAD=true
          export OPENSCIENCE_DISABLE_DEFAULT_PLUGINS=true
          export OPENSCIENCE_EXPERIMENTAL_DISABLE_FILEWATCHER=true
          npm install --global --prefix "$prefix" "@synsci/openscience@${{ needs.version.outputs.version }}"
          bin="$prefix/bin/openscience"
          "$bin" --version | tee "$RUNNER_TEMP/openscience-version.txt"
          grep -Fx "${{ needs.version.outputs.version }}" "$RUNNER_TEMP/openscience-version.txt"
          "$bin" serve --port 4096 > "$RUNNER_TEMP/openscience-smoke.log" 2>&1 &
          pid=$!
          trap 'kill "$pid" 2>/dev/null || true' EXIT
          for _ in $(seq 1 90); do
            curl -fsS "http://127.0.0.1:4096/global/health" > /dev/null && exit 0
            sleep 1
          done
          echo "::error::openscience server never became healthy"
          cat "$RUNNER_TEMP/openscience-smoke.log" || true
          exit 1
      - name: Verify compiled macOS worker startup with piped stdin
        if: runner.os == 'macOS'
        shell: bash
        run: |
          set -euo pipefail
          modules="$RUNNER_TEMP/openscience-prefix/lib/node_modules"
          native=$(node --input-type=module - "$modules" <<'NODE'
          import path from "node:path"
          import { createRequire } from "node:module"
          const require = createRequire(path.join(process.argv[2], "@synsci/openscience/package.json"))
          const manifest = require.resolve(`@synsci/openscience-darwin-${process.arch}/package.json`)
          console.log(path.join(path.dirname(manifest), "bin/openscience"))
          NODE
          )
          bun --no-env-file backend/cli/script/smoke-darwin-launcher.ts "$native" "$RUNNER_TEMP/darwin-launcher-smoke"
      - name: Upload macOS worker startup evidence
        if: always() && runner.os == 'macOS'
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: npm-test-darwin-launcher-${{ github.run_attempt }}
          if-no-files-found: error
          include-hidden-files: true
          retention-days: 30
          path: ${{ runner.temp }}/darwin-launcher-smoke
      - name: Install and smoke the OpenScience candidate
        if: runner.os == 'Windows'
        shell: pwsh
        run: |
          $prefix = Join-Path $env:RUNNER_TEMP "openscience-prefix"
          $env:OPENSCIENCE_DISABLE_SHARE = "true"
          $env:OPENSCIENCE_DISABLE_LSP_DOWNLOAD = "true"
          $env:OPENSCIENCE_DISABLE_DEFAULT_PLUGINS = "true"
          $env:OPENSCIENCE_EXPERIMENTAL_DISABLE_FILEWATCHER = "true"
          npm install --global --prefix $prefix "@synsci/openscience@${{ needs.version.outputs.version }}"
          $bin = Join-Path $prefix "openscience.cmd"
          & $bin --version | Tee-Object -FilePath (Join-Path $env:RUNNER_TEMP "openscience-version.txt")
          $actual = Get-Content (Join-Path $env:RUNNER_TEMP "openscience-version.txt")
          if ($actual -ne "${{ needs.version.outputs.version }}") {
            throw "expected version ${{ needs.version.outputs.version }}, got $actual"
          }
          $stdout = Join-Path $env:RUNNER_TEMP "openscience-smoke.out.log"
          $stderr = Join-Path $env:RUNNER_TEMP "openscience-smoke.err.log"
          $proc = Start-Process -FilePath $bin -ArgumentList @("serve", "--port", "4096") -RedirectStandardOutput $stdout -RedirectStandardError $stderr -PassThru
          try {
            foreach ($i in 1..90) {
              try {
                Invoke-WebRequest -UseBasicParsing "http://127.0.0.1:4096/global/health" | Out-Null
                exit 0
              } catch {
                Start-Sleep -Seconds 1
              }
            }
            Get-Content $stdout -ErrorAction SilentlyContinue
            Get-Content $stderr -ErrorAction SilentlyContinue
            throw "openscience server never became healthy"
          } finally {
            Stop-Process -Id $proc.Id -Force -ErrorAction SilentlyContinue
          }

  musl-baseline-smoke:
    needs:
      - version
      - build-cli
    if: inputs.run_os_smoke
    runs-on: ubuntu-latest
    timeout-minutes: 25
    # node:24-alpine multi-arch index as of 2026-09-03
    container: node:24-alpine@sha256:e67514e5d0f6c46656005e1b693b2ec9d52e80b641307de684d4a015ba7a4eaf
    steps:
      - name: Install Alpine smoke dependencies
        shell: sh
        run: apk add --no-cache curl libstdc++
      - name: Download the packed candidate
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          name: npm-candidate-${{ needs.version.outputs.version }}
          path: ${{ runner.temp }}/npm-candidate
      - name: Install and smoke the exact x64 musl baseline
        shell: sh
        run: |
          set -eu
          prefix="$RUNNER_TEMP/openscience-musl-baseline"
          # The platform package has no dependencies, so the tarball itself is
          # the whole install.
          tarball=$(node -e '
            const manifest = require(process.argv[1] + "/manifest.json")
            const found = manifest.artifacts.find((item) => item.name === "@synsci/openscience-linux-x64-baseline-musl")
            if (!found) throw new Error("candidate lacks the x64 musl baseline package")
            console.log(require("path").join(process.argv[1], found.file))
          ' "$RUNNER_TEMP/npm-candidate")
          npm install --prefix "$prefix" "$tarball"
          bin="$prefix/node_modules/@synsci/openscience-linux-x64-baseline-musl/bin/openscience"
          "$bin" --version > "$RUNNER_TEMP/openscience-musl-version.txt"
          grep -Fx "${{ needs.version.outputs.version }}" "$RUNNER_TEMP/openscience-musl-version.txt"
          OPENSCIENCE_DISABLE_SHARE=true \
            OPENSCIENCE_DISABLE_LSP_DOWNLOAD=true \
            OPENSCIENCE_DISABLE_DEFAULT_PLUGINS=true \
            OPENSCIENCE_EXPERIMENTAL_DISABLE_FILEWATCHER=true \
            "$bin" serve --port 4096 > "$RUNNER_TEMP/openscience-musl-smoke.log" 2>&1 &
          pid=$!
          trap 'kill "$pid" 2>/dev/null || true' EXIT
          i=0
          while [ "$i" -lt 90 ]; do
            if curl -fsS "http://127.0.0.1:4096/global/health" > /dev/null; then
              exit 0
            fi
            i=$((i + 1))
            sleep 1
          done
          echo "musl baseline server never became healthy" >&2
          sed -n '1,200p' "$RUNNER_TEMP/openscience-musl-smoke.log" >&2 || true
          exit 1

  # One job per capability x native runner. Each packaged lifecycle takes about
  # three minutes, so fanning out cuts the rehearsal's critical path by ten to
  # fifteen minutes. publish.yml's rehearsal gate expects every combination
  # below by exact job name; change both files together.
  scientific-capability-canary:
    needs:
      - version
      - build-cli
    if: inputs.run_scientific_canary
    strategy:
      fail-fast: false
      matrix:
        os: [linux-x64, linux-arm64, macos-arm64]
        capability: [scipy, matplotlib, scikit-learn, biopython, rdkit]
        include:
          - os: linux-x64
            runner: ubuntu-latest
          - os: linux-arm64
            runner: ubuntu-24.04-arm
          - os: macos-arm64
            runner: macos-latest
    name: scientific capability canary (${{ matrix.os }}, ${{ matrix.capability }})
    runs-on: ${{ matrix.runner }}
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          ref: ${{ needs.version.outputs.source }}
      - uses: ./.github/actions/setup-bun
        with:
          install: "false"
      - uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7.0.0
        with:
          node-version: "24"
      - uses: ./.github/actions/candidate-registry
        with:
          version: ${{ needs.version.outputs.version }}
      - name: Install and prove the Linux capability sandbox
        if: runner.os == 'Linux'
        shell: bash
        run: |
          set -euo pipefail
          sudo apt-get update
          sudo apt-get install --yes bubblewrap
          if [[ -e /proc/sys/kernel/apparmor_restrict_unprivileged_userns ]]; then
            echo 0 | sudo tee /proc/sys/kernel/apparmor_restrict_unprivileged_userns
          fi
          bwrap --ro-bind / / --dev /dev --proc /proc --unshare-pid --die-with-parent -- true
      - name: Install the exact packed OpenScience candidate
        shell: bash
        run: |
          set -euo pipefail
          prefix="$RUNNER_TEMP/openscience-capability-prefix"
          mkdir -p "$prefix"
          npm install --global --prefix "$prefix" "@synsci/openscience@${{ needs.version.outputs.version }}"
          echo "$prefix/bin" >> "$GITHUB_PATH"
          "$prefix/bin/openscience" --version | tee "$RUNNER_TEMP/openscience-capability-version.txt"
          grep -Fx "${{ needs.version.outputs.version }}" "$RUNNER_TEMP/openscience-capability-version.txt"
      - name: Run the packaged scientific capability lifecycle
        shell: bash
        env:
          CANARY_CAPABILITY: ${{ matrix.capability }}
          HOME: ${{ runner.temp }}/openscience-capability/home
          XDG_DATA_HOME: ${{ runner.temp }}/openscience-capability/share
          XDG_CACHE_HOME: ${{ runner.temp }}/openscience-capability/cache
          XDG_CONFIG_HOME: ${{ runner.temp }}/openscience-capability/config
          XDG_STATE_HOME: ${{ runner.temp }}/openscience-capability/state
          OPENSCIENCE_CONFIG_DIR: ${{ runner.temp }}/openscience-capability/config/openscience
          OPENSCIENCE_DATA_DIR: ${{ runner.temp }}/openscience-capability/data
          OPENSCIENCE_DISABLE_SHARE: "true"
          OPENSCIENCE_DISABLE_LSP_DOWNLOAD: "true"
          OPENSCIENCE_DISABLE_DEFAULT_PLUGINS: "true"
          OPENSCIENCE_EXPERIMENTAL_DISABLE_FILEWATCHER: "true"
          OPENSCIENCE_RELEASE_SHA: ${{ needs.version.outputs.source }}
        run: |
          set -euo pipefail
          project="$RUNNER_TEMP/openscience-capability/project"
          mkdir -p "$HOME" "$XDG_DATA_HOME" "$XDG_CACHE_HOME" "$XDG_CONFIG_HOME" "$XDG_STATE_HOME" "$OPENSCIENCE_DATA_DIR" "$project"
          cd "$project"
          openscience --print-logs debug capability-canary "$CANARY_CAPABILITY" --target local --timeout 900 \
            2> >(tee "$RUNNER_TEMP/scientific-capability-canary.log" >&2) \
            | tee "$RUNNER_TEMP/scientific-capability-canary.json"
          CANARY_RESULT="$RUNNER_TEMP/scientific-capability-canary.json" \
          EXPECTED_CAPABILITY="$CANARY_CAPABILITY" \
          EXPECTED_SOURCE="${{ needs.version.outputs.source }}" \
          EXPECTED_VERSION="${{ needs.version.outputs.version }}" \
          node --input-type=module - <<'NODE'
          import fs from "node:fs"
          const report = JSON.parse(fs.readFileSync(process.env.CANARY_RESULT, "utf8"))
          if (report.schema_version !== 1 || report.target !== "local") throw new Error("invalid canary report")
          if (!Array.isArray(report.results) || report.results.length !== 1) {
            throw new Error(`expected exactly one packaged capability result, received ${report.results?.length}`)
          }
          const item = report.results[0]
          if (item.capability !== process.env.EXPECTED_CAPABILITY) {
            throw new Error(`expected the ${process.env.EXPECTED_CAPABILITY} release canary, received ${item.capability}`)
          }
          if (item.status !== "succeeded") throw new Error(`${item.capability} ended ${item.status}`)
          if (item.doctor?.availability?.local !== "ready") {
            throw new Error(`${item.capability} doctor did not report the exact local pack ready`)
          }
          const evidence = item.verification?.evidence
          if (evidence?.release_sha !== process.env.EXPECTED_SOURCE) {
            throw new Error(`${item.capability} recorded the wrong release source`)
          }
          if (evidence?.app_version !== process.env.EXPECTED_VERSION) {
            throw new Error(`${item.capability} recorded the wrong candidate version`)
          }
          if (!item.verification?.validation?.ok) throw new Error(`${item.capability} failed semantic validation`)
          console.log(`verified the exact packaged ${item.capability} lifecycle`)
          NODE
      - name: Upload scientific capability evidence
        if: always()
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: npm-test-scientific-capability-${{ matrix.os }}-${{ matrix.capability }}-${{ github.run_attempt }}
          if-no-files-found: ignore
          retention-days: 30
          path: |
            ${{ runner.temp }}/scientific-capability-canary.json
            ${{ runner.temp }}/scientific-capability-canary.log
            ${{ runner.temp }}/openscience-capability/data/scientific-capability-evidence.json
            ${{ runner.temp }}/openscience-capability/data/log
            ${{ runner.temp }}/openscience-capability/data/compute/projects
            ${{ runner.temp }}/openscience-capability/data/conda/state.json
