Use the `apply_patch` tool to edit files. Your patch language is a stripped‑down, file‑oriented diff format designed to be easy to parse and safe to apply. You can think of it as a high‑level envelope:

*** Begin Patch
[ one or more file sections ]
*** End Patch

Within that envelope, you get a sequence of file operations. Multi-file patches are supported in one call: the harness preflights every file before writing, requests approval for the complete diff, and rolls back completed file operations if a later operation fails.
You MUST include a header to specify the action you are taking.
Each operation starts with one of three headers:

*** Add File: <path> - create a new file. Every following line is a + line (the initial contents).
*** Delete File: <path> - remove an existing file. Nothing follows.
*** Update File: <path> - patch an existing file in place (optionally with a rename).

Example patch:

```
*** Begin Patch
*** Add File: hello.txt
+Hello world
*** Update File: src/app.py
*** Move to: src/main.py
@@ def greet():
-print("Hi")
+print("Hello, world!")
*** Delete File: obsolete.txt
*** End Patch
```

It is important to remember:

- You must include a header with your intended action (Add/Delete/Update)
- You must prefix new lines with `+` even when creating a new file
- Update hunks match complete physical lines. Re-read long prose or LaTeX lines before editing them; a sentence fragment is not a removable line.
- To replace a whole existing file, use one Update File section removing its complete current contents and adding the replacement. Do not delete it in one call and recreate it later: other workers may read it between calls. On macOS/Linux, adjacent Delete File + Add File sections for the same path are also treated as a single guarded atomic replacement; platforms without atomic exchange reject that pair. Ordinary Update remains available on every platform.
- A formatter may change the saved contents after your edit. The result records the saved content, diff and SHA-256; when it differs from your proposal, the output includes the formatting changes (or asks you to re-read a large result). Use the saved contents for the next patch.
- If verification fails, use the reported failed-hunk number, current SHA-256, and bounded context to construct a revised patch. An unchanged duplicate is blocked, while a materially revised patch is safely preflighted again.
