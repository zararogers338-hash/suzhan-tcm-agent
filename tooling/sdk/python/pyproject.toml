[project]
name = "openscience-runtime"
version = "0.1.0"
description = "Standard-library Python client for the OpenScience runtime HTTP protocol"
readme = "README.md"
license = "Apache-2.0"
requires-python = ">=3.10"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["openscience"]
