NATIVE_HARBOR_CONFORMANCE: Read `fixture-input.json`, multiply its integer `a`
and `b`, and write `fixture-result.json` with the result as `value` and the
current working directory as `cwd`. Use a local tool, then report completion.
